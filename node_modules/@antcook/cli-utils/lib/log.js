const chalk = require('chalk')

module.exports = {
  success(info) {
    console.info(
      chalk.green(info)
    )
  },
  fail(info) {
    console.error(
      chalk.red(info)
    )
  },
  warining(info) {
    console.error(
      chalk.yellow(info)
    )
  },
}